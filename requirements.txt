# Pipeline core
python-dotenv
pydantic>=2.0
nbformat
markdown
Pillow

# Report generation (Phases A–D call the Anthropic API directly)
anthropic

# Slack bot
slack-bolt
slack-sdk

# LabArchives cookie refresh (headed browser login)
playwright

# Optional: read session cookies from installed browser instead of get_la_cookies.py
# browser-cookie3

# Lab-specific packages
# presto: not on PyPI — install from the bundled wheel:
#   pip install presto-2.16.0-py3-none-any.whl
# daq: WashU internal wrapper around presto
daq @ git+https://github.com/WashU-Astroparticle-Lab/daq.git
